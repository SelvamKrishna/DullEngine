#pragma once

#include <cstdint>

namespace EngineInfo {
	inline constexpr uint8_t VERSION_MAJOR { 0 };
	inline constexpr uint8_t VERSION_MINOR { 1 };
	inline constexpr uint8_t COMMIT { 4 }; // should match total no.of commits
}