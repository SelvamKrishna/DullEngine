# Todo

---

## impl: Render System

Processing elements provide Render System with needed Render Calls. Render System should support abstraction similar to Nodes allowing smooth integration of custom Render Calls into engine. Developer is
required to write their own Render System (or use pre-existing templates).

Eg: SpriteSheet Renderer, UI Renderer, Texture Renderer.

---
